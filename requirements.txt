# Core
PyYAML>=6.0
rapidfuzz>=3.0        # fast fuzzy string matching for name detection

# Screen capture + image preprocessing
mss>=9.0
numpy>=1.24
opencv-python>=4.8

# OCR — pick ONE engine (see config.yaml: ocr_engine)
#   easyocr uses your NVIDIA GPU via torch (heavier install, more robust)
easyocr>=1.7
#   tesseract is lighter but needs the Tesseract binary installed separately
#   (Windows: https://github.com/UB-Mannheim/tesseract/wiki)
pytesseract>=0.3.10

# OBS control (obs-websocket v5)
obsws-python>=1.6

# Match cards, reel title/POTG cards, shorts labels
Pillow>=10.0

# Human-sounding announcer voice for reels (Microsoft neural TTS; needs
# internet at reel-build time — falls back to the offline voice without it)
edge-tts>=7.0

# Optional: system-tray skull icon (run with --tray).
pystray>=0.19

# Optional: YouTube auto-upload of the session reel (see README). Only needed
# if youtube_upload_session_reel: true in config.yaml.
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
pywebview
